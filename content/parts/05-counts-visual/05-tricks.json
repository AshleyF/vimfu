{
  "format": "vimfu-content-topic",
  "version": 1,
  "id": "counts-visual.tricks",
  "title": "Visual Mode Tricks",
  "subtitle": "Block O for diagonal corners. gv to re-select.",
  "part": "counts-visual",
  "summary": "Two tricks that make Visual mode much more useful: O in block mode swaps to the opposite diagonal corner, and gv re-selects the previous selection.",
  "keys": ["O", "gv"],
  "lessons": [69, 70],
  "see_also": ["counts-visual.line-block", "counts-visual.char-visual", "visual.gv"],

  "blocks": [
    { "type": "heading", "level": 1, "text": "Visual Mode Tricks" },

    { "type": "prose", "text": "Two small tricks save lots of pain in Visual mode: {key:O} (capital O) for moving across the diagonal of a block, and {key:g}{key:v} for getting back the selection you just lost." },

    { "type": "heading", "level": 2, "text": "O — diagonal corners" },

    { "type": "prose", "text": "In char/line Visual, {key:o} swaps which end of the selection your motions move. In *block* Visual, {key:o} swaps along one diagonal — but {key:O} (capital) swaps to the *opposite* corner of the rectangle. So you can extend the block from any of the four corners without restarting." },

    { "type": "keys", "sequence": ["O"], "label": "(in Block Visual) Move cursor to opposite corner of rectangle" },

    { "type": "embed", "lesson": 69, "caption": "O — block-visual diagonal" },

    { "type": "heading", "level": 2, "text": "gv — re-select last visual" },

    { "type": "prose", "text": "Pressed Esc by accident? Did one operation and want to do another on the same range? {key:g}{key:v} restores the last visual selection — same anchor, same cursor, same mode." },

    { "type": "keys", "sequence": ["g","v"], "label": "Re-select previous visual range" },

    { "type": "embed", "lesson": 70, "caption": "gv — re-select previous visual" },

    {
      "type": "internals",
      "title": "How gv Works",
      "text": "Whenever you leave Visual mode, Vim writes two marks ({key:`}{key:<}, {key:`}{key:>}) and remembers the *type* of the selection. {key:g}{key:v} re-enters that type of Visual with the cursor and anchor restored from those marks.\n\nBecause it's just two marks, anything that updates them updates {key:g}{key:v}'s target. {key::}{key:s} commands run on the visual range update them implicitly — so you can {key::}{key:'}{key:<}{key:,}{key:'}{key:>}{key:s}{key:/}{key:foo}{key:/}{key:bar}{key:/}{key:g} and then {key:g}{key:v} to grab the same range again.\n\nThe range works *per buffer*. Open a different buffer; come back; {key:g}{key:v} still works."
    },

    { "type": "divider" },
    { "type": "heading", "level": 2, "text": "Reference" },
    {
      "type": "table",
      "headers": ["Key",          "Action"],
      "rows": [
        ["{key:o}",                "Swap cursor / anchor (any visual)"],
        ["{key:O}",                "Move to opposite corner (block visual only)"],
        ["{key:g}{key:v}",         "Re-enter previous visual selection"]
      ]
    },

    { "type": "qr", "topic": "counts-visual.tricks" },
    { "type": "buy-prompt" }
  ]
}
