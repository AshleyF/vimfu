{
  "format": "vimfu-content-topic",
  "version": 1,
  "id": "advanced.gx-g-less",
  "title": "{key:gx} and {key:g<}",
  "subtitle": "Open URLs and recall command output.",
  "part": "advanced",
  "summary": "{key:g}{key:x} opens the URL or filename under cursor in the system handler (browser/etc.). {key:g}{key:<} re-displays the output of the last `:command` — handy when a long message scrolled past.",
  "keys": [
    "{key:g}{key:x}",
    "{key:g}{key:<}"
  ],
  "lessons": [
    493,
    494
  ],
  "see_also": [
    "prefixes.g-overview"
  ],
  "blocks": [
    {
      "type": "heading",
      "level": 1,
      "text": "{key:g}{key:x} and {key:g}{key:<}"
    },
    {
      "type": "prose",
      "text": "Two small {key:g} commands worth knowing — both solve real problems."
    },
    {
      "type": "heading",
      "level": 2,
      "text": "{key:g}{key:x} — Open URL"
    },
    {
      "type": "prose",
      "text": "{key:g}{key:x} on a URL or file path passes it to the system's default opener (`open` on macOS, `xdg-open` on Linux, `cmd /c start` on Windows). Browsers open URLs; PDFs open in PDF readers; image files open in image viewers."
    },
    {
      "type": "internals",
      "title": "How {key:g}{key:x} Decides What to Open",
      "text": "The implementation is in `$VIMRUNTIME/plugin/netrwPlugin.vim` — Vim's netrw plugin defines {key:g}{key:x} as a *normal mode* mapping that calls `netrw#BrowseX(netrw#GX(), 0)`.\n\nThe URL/path is found by looking at the WORD under the cursor (not just word) — so URLs with `.`, `?`, `&`, etc. are captured whole. The handler is selected by `browser` if set, otherwise by platform defaults."
    },
    {
      "type": "heading",
      "level": 2,
      "text": "{key:g}{key:<} — Re-show Last Output"
    },
    {
      "type": "prose",
      "text": "{key:g}{key:<} reopens the message area showing the output of the *last* Ex command — handy when a `:registers` or `:set all` listing scrolled past faster than you could read."
    },
    {
      "type": "internals",
      "title": "Where Messages Live",
      "text": "Vim keeps a rolling buffer of message-area output. `:messages` shows the recent history; {key:g}{key:<} brings back the most recent block.\n\nAfter {key:g}{key:<}, the message hit-Enter prompt appears so you can scroll the listing with {key:Space}/{key:b}, then dismiss with {key:Enter}."
    },
    {
      "type": "qr",
      "topic": "advanced.gx-g-less"
    },
    {
      "type": "buy-prompt"
    }
  ]
}