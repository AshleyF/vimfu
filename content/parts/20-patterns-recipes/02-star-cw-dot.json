{
  "format": "vimfu-content-topic",
  "version": 1,
  "id": "patterns.star-cw-dot",
  "title": "The Replace Loop — {key:*}, {key:cw}, then {key:.} {key:.} {key:.}",
  "subtitle": "Search a word, change one, repeat with dot.",
  "part": "patterns-recipes",
  "summary": "{key:*}{key:c}{key:w}`NEW`{key:Esc}, then {key:n}{key:.}{key:n}{key:.} — change every occurrence of a word, one at a time, with confirmation. The most-loved Vim refactoring loop.",
  "keys": [
    "{key:*}",
    "{key:c}",
    "{key:w}",
    "{key:n}",
    "{key:.}"
  ],
  "lessons": [
    463,
    464
  ],
  "see_also": [
    "search.word",
    "dot.repeat",
    "patterns.cgn-dot"
  ],
  "blocks": [
    {
      "type": "heading",
      "level": 1,
      "text": "{key:*}, {key:c}{key:w}, then {key:.} {key:.} {key:.} — The Replace Loop"
    },
    {
      "type": "prose",
      "text": "The classic Vim refactoring loop. Searches for a word, changes one instance, then jumps to each next match where you decide whether to dot-repeat (apply) or skip."
    },
    {
      "type": "keys",
      "label": "Step by step",
      "sequence": [
        {
          "key": "{key:*}",
          "note": "Cursor on word: search next exact match"
        },
        {
          "key": "{key:c}{key:w}",
          "note": "Change word — Vim drops you in *insert mode*"
        },
        {
          "key": "`NEW`",
          "note": "Type replacement"
        },
        {
          "key": "{key:Esc}",
          "note": "Back to *normal mode* — change is saved as the dot-target"
        },
        {
          "key": "{key:n}",
          "note": "Jump to next match"
        },
        {
          "key": "{key:.}",
          "note": "Apply the same change"
        },
        {
          "key": "{key:n}",
          "note": "Skip this one — just move to next"
        },
        {
          "key": "{key:.}",
          "note": "Apply…"
        }
      ]
    },
    {
      "type": "internals",
      "title": "Why It's Better Than :%s",
      "text": "`:%s/old/new/gc` also works — but you have to type both `old` and `new`, and you confirm at the *line level* (yes/no). The {key:*}{key:c}{key:w} loop is faster:\n\n  • {key:*} fills in the search pattern for you (the word under cursor with `\\<...\\>` boundaries).\n  • {key:c}{key:w} drops you straight into *insert mode* at the word's position.\n  • {key:Esc} commits the change. The {key:.} register now holds *the entire change*, including motion and inserted text.\n  • {key:n} + {key:.} cycle through matches, applying or skipping at each.\n\nThe loop also handles cases where the matches differ slightly (different surrounding context, decision per-match) — much more flexible than substitute."
    },
    {
      "type": "tip",
      "text": "Need word boundaries to be loose? Use {key:g}{key:*} instead of {key:*} — finds the word as a substring, not just whole-word matches."
    },
    {
      "type": "example",
      "ref": "pattern.star-cw-dot"
    },
    {
      "type": "qr",
      "topic": "patterns.star-cw-dot"
    },
    {
      "type": "buy-prompt"
    }
  ]
}