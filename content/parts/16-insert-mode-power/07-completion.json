{
  "format": "vimfu-content-topic",
  "version": 1,
  "id": "insert.completion",
  "title": "Word Completion (Ctrl-N / Ctrl-P)",
  "subtitle": "Vim has had autocomplete since 1996.",
  "part": "insert-mode-power",
  "summary": "Ctrl-N and Ctrl-P trigger keyword completion from the current and other buffers. Once a menu is up, Ctrl-N/Ctrl-P walk the suggestions; Ctrl-E cancels; Ctrl-Y accepts.",
  "keys": ["Ctrl-N", "Ctrl-P", "Ctrl-E", "Ctrl-Y"],
  "lessons": [384, 388],
  "see_also": ["insert.ctrl-x-completion"],

  "blocks": [
    { "type": "heading", "level": 1, "text": "Word Completion" },

    { "type": "prose", "text": "Type a partial word, then {key:Ctrl-N} (next) or {key:Ctrl-P} (previous) to autocomplete from words already in scope. Vim has had this since the mid-90s — long before IDE autocomplete was standard." },

    {
      "type": "keys",
      "label": "Workflow",
      "sequence": [
        { "key": "tex",      "note": "Start typing…" },
        { "key": "Ctrl-N",   "note": "Pop the completion menu" },
        { "key": "Ctrl-N",   "note": "Cycle forward through matches" },
        { "key": "Ctrl-P",   "note": "Cycle back" },
        { "key": "Ctrl-Y",   "note": "Accept the highlighted match" },
        { "key": "Ctrl-E",   "note": "Cancel — keep what you typed" }
      ]
    },

    {
      "type": "internals",
      "title": "Where Suggestions Come From",
      "text": "{key:Ctrl-N}/{key:Ctrl-P} use the `'complete'` option (default `.,w,b,u,t`) to decide which sources to scan:\n\n  • `.` — current buffer\n  • `w` — buffers in other windows\n  • `b` — other loaded buffers\n  • `u` — unloaded buffers\n  • `t` — tags\n  • `i` — included files (via `'include'` regex)\n  • `k` — dictionary file\n\nMatches are scanned in the order listed, deduplicated, and shown in a popup menu (`'completeopt'` controls how). The menu navigation keys ({key:Ctrl-N}/{key:Ctrl-P}/{key:Ctrl-Y}/{key:Ctrl-E}) only apply *while the menu is visible*."
    },

    { "type": "tip", "text": "The default *just works* for code: pulls from the file you're editing plus open files. For something more powerful, see {key:Ctrl-X} sub-completions next." },

    { "type": "qr", "topic": "insert.completion" },
    { "type": "buy-prompt" }
  ]
}
